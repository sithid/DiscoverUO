@using DiscoverUO.Lib.Shared.Servers
@using Blazored.LocalStorage
@using System.Net
@inject ILocalStorageService _localStorage
@inject HttpClient _client

@page "/servers"

<PageTitle>Servers</PageTitle>

<MudContainer>
    <MudLabel style="font-size: 32px">This is where the public server list content will be.</MudLabel>
</MudContainer>

@code{

    public List<ServerData> _serverlist{ get; set; }

    protected override async Task OnInitializedAsync()
    {
        _serverlist = await DataManager.GetPublicServers(_client, _localStorage);
        base.OnInitializedAsync();
    }
}